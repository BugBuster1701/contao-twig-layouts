# Hinweis
Da die Contao Demo mit SCSS arbeitet, die serverseitig in CSS compiliert wird, wurde hier die compilierte CSS in files/... kopiert und dann eingebunden.<br>
(contaodemo/files_contaodemo_theme_src_scss_app.scss.css)

Die TWIG Layout Varianten liegen im Verzeichnis /templates/page/layout, angelegt durch das Template Studio.
